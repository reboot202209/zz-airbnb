import styled from "styled-components";

const LeftWrapper = styled.div`
  flex: 1;

  .logo {
    width: 145px;
    height: 80px;
    display: flex;
    align-items: center;
    cursor: pointer;
  }
`

export default LeftWrapper